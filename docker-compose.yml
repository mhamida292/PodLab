services:
  podlab:
    build: .
    image: podlab:latest
    container_name: podlab
    restart: unless-stopped
    ports:
      - "9090:9090"
    environment:
      - PORT=9090
      # Override the feed without rebuilding, if you want:
      # - FEED_URL=https://feeds.feedburner.com/QalamPodcast
